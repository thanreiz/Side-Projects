package com.floapp.agriflo.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Palette — Agricultural Earth Tones ─────────────────────────────
val FloGreen700 = Color(0xFF2E7D32)       // Deep forest green — primary
val FloGreen500 = Color(0xFF4CAF50)       // Mid green — buttons, accents
val FloGreen200 = Color(0xFFA5D6A7)       // Soft green — surfaces
val FloGreen100 = Color(0xFFC8E6C9)       // Very light green — chip backgrounds
val FloGreen50  = Color(0xFFE8F5E9)       // Near white green — backgrounds

val FloEarth700 = Color(0xFF5D4037)       // Dark earth brown — secondary
val FloEarth500 = Color(0xFF795548)       // Mid brown — secondary accents
val FloEarth200 = Color(0xFFBCAAA4)       // Soft brown — dividers
val FloEarth50  = Color(0xFFEFEBE9)       // Near white brown

val FloGold700  = Color(0xFFF57F17)       // Deep gold — warning text/icons
val FloGold500  = Color(0xFFF9A825)       // Harvest gold — warnings, harvest alerts
val FloGold200  = Color(0xFFFFE082)       // Soft gold — highlight surfaces
val FloGold100  = Color(0xFFFFF9C4)       // Very light gold — info banners

val FloRed500   = Color(0xFFC62828)       // Alert red — critical warnings
val FloRed100   = Color(0xFFFFCDD2)       // Soft red — error surfaces

val FloBlue700  = Color(0xFF1565C0)       // Deep sky blue — rain text
val FloBlue500  = Color(0xFF1976D2)       // Sky blue — water/rain indicators
val FloBlue100  = Color(0xFFBBDEFB)       // Light blue — rain surfaces
val FloBlue50   = Color(0xFFE3F2FD)       // Near white blue — permission card

// ─── Neutral ────────────────────────────────────────────────────────────────
val FloGray900  = Color(0xFF212121)       // Near black — primary text
val FloGray700  = Color(0xFF424242)       // Dark — secondary text
val FloGray400  = Color(0xFF9E9E9E)       // Medium — disabled, hints
val FloGray100  = Color(0xFFF5F5F5)       // Light — card surfaces
val FloWhite    = Color(0xFFFFFFFF)

// ─── Semantic ────────────────────────────────────────────────────────────────
val FloSurface       = Color(0xFFFAFFF9)  // Off-white with green tint
val FloOnSurface     = FloGray900
val FloBackground    = Color(0xFFF1F8F0)  // Pale green background
val FloOnBackground  = FloGray900
