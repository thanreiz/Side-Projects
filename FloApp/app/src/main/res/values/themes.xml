<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal XML theme — Compose handles all actual theming via FloTheme.
         Parent must be NoActionBar to avoid title bar appearing under Compose. -->
    <style name="Theme.FloApp" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/flo_green_dark</item>
    </style>
</resources>

